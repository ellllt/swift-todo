import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Text("SampleProject")
                .padding()
            Text("Actual Machine Test")
                .padding()
        }
    }
}

struct ContentView_Previews: PreviewProvider {
    static var previews: some View {
        ContentView()
    }
}
